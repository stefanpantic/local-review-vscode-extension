name: Bug report
description: Something isn't working as expected
labels: [bug]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for filing a bug. Please don't paste private code you can't share.
  - type: textarea
    id: what-happened
    attributes:
      label: What happened?
      description: A clear description of the bug and what you expected to happen instead.
    validations:
      required: true
  - type: textarea
    id: repro
    attributes:
      label: Steps to reproduce
      placeholder: |
        1. Open ReviewMate on a repo with …
        2. …
        3. See …
    validations:
      required: true
  - type: input
    id: versions
    attributes:
      label: Versions
      description: ReviewMate version, VS Code version, and OS.
      placeholder: ReviewMate 0.8.1 · VS Code 1.90 · macOS 14
    validations:
      required: true
  - type: textarea
    id: logs
    attributes:
      label: Logs (optional)
      description: Output from the "ReviewMate" output channel (enable `agenticReview.log`). Redact anything sensitive.
      render: shell
